import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coliv.manager',
  appName: 'coliv_manager',
  webDir: 'dist'
};

export default config;
